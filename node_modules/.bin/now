#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../now/build/bin/now" "$@"
  ret=$?
else 
  node  "$basedir/../now/build/bin/now" "$@"
  ret=$?
fi
exit $ret
